import { ColDef } from 'ag-grid-community'

const DEFAULT_COLUMN_DEFS: ColDef = {
  autoHeight: true,
  editable: false,
  enableRowGroup: false,
  filter: true,
  flex: 1,
  resizable: true,
  sortable: true,
  suppressMenu: true,
  width: 100,
  wrapText: true,
}

export default DEFAULT_COLUMN_DEFS
