import dayjs from 'dayjs'

function getDateFromYearString(yearString: string): string {
  const year = parseInt(yearString)

  const date = dayjs().year(year).format('YYYY-MM-DD')
  return date
}

export default getDateFromYearString
