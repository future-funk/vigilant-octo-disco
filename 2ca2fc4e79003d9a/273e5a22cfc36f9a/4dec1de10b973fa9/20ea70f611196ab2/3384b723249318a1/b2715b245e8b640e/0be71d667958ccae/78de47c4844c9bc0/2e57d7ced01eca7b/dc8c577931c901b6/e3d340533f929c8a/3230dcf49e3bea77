import { useAppPalette } from '@bpd/bpd-web/shared/theme'

const useGetLegendColors = () => {
  const palette = useAppPalette()

  const colors = {
    'Dry Storage': palette.map.orange,
    'Cold Storage': palette.map.blue,
    Mixed: palette.map.green,
    Land: palette.map.red,
    Construction: palette.map.orange,
    Completed: palette.map.green,
    Approved: palette.map.green,
    Closed: palette.map.orange,
    Dropped: palette.map.red,
    'T1(S)': palette.map.maroon,
    'T1(N)': palette.map.salmon,
    'T1(W)': palette.map.red,
    'T1.5(S)': palette.map.darkBlue,
    'T1.5(N)': palette.map.darkSkyBlue,
    'T1.5(W)': palette.map.blue,
    'T2(S)': palette.map.green,
    'T2(N)': palette.map.pistachio,
    'T2(W)': palette.map.aqua,
    'Other Tiers': palette.map.orange,
    GLP: palette.map.aqua,
    VX: palette.map.red,
    JD: palette.map.blue,
    Gaw: palette.map.sanMarino,
    ESR: palette.map.biceBlue,
    Logos: palette.map.verdigris,
    Chelsfield: palette.map.green,
    'Keppel/Blogis': palette.map.magentaHaze,
    SF: palette.map.yellow,
    Brilliant: palette.map.orange,
    'New Ease': palette.map.jasper,
    CBREGI: palette.map.persianRed,
    'Sino Ocean': palette.map.mediumSlateBlue,
    Metcold: palette.map.electricIndigo,
  }

  return colors
}

export default useGetLegendColors
